---
import { computeRenderedInsights } from "../../components/insights/computeRenderedInsights";
import { InsightsPage } from "../../components/insights/InsightsPage";
import BaseLayout from "../../layouts/BaseLayout.astro";

const initialResults = computeRenderedInsights();

const insightsDescription =
  "Explore Eurovision YouTube Top 20 insights: build-up ranks before the contest, winners that never hit #1, enduring classics, and more — from official Most Watched chart points.";
---

<BaseLayout
  current="insights"
  description={insightsDescription}
  title="Insights"
>
  <InsightsPage client:visible initialResults={initialResults} />
</BaseLayout>
